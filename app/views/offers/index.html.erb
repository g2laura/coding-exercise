<h1>Offers#index</h1>

<p>
  <%= link_to('New', new_offer_path) %>
</p>

<ul>
  <% @offers.each do |o| %>
    <li>Offer <%= o.grade %>, <%= o.price %> x <%= o.quantity %> (<%= o.season %>) <%= link_to('Edit', edit_offer_path(o.id)) %></li>
  <% end %>
</ul>
